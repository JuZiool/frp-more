# FRP-More 项目规则

## 1. 项目定位

FRP-More 是一个 Go 编写的 FRP 客户端可视化管理面板：

- 一个进程内运行多个 frpc 实例。
- 每个实例对应 `data/instances/<实例名>.toml`。
- 通过内嵌的 FRP `client.Service` 管理实例，不启动外部 `frpc` 子进程。
- 前端是嵌入 Go 二进制的单文件页面：`internal/server/static/index.html`。

## 2. 核心依赖与兼容性

- Go 版本要求：Go 1.25 或更高版本。
- FRP 版本固定为 `github.com/fatedier/frp v0.71.0`。
- `frp/client` 属于 FRP 内部 API，升级 FRP 必须同时进行回归测试。
- 不要为了方便改成执行外部 `frpc` 命令，除非明确改变项目架构。

## 3. 配置处理规则

- 只接受 TOML 实例配置；实例目录扫描只识别 `.toml` 文件。
- 保存配置前必须走与官方 frpc 一致的加载、聚合、补全和校验流程。
- 不得在保存过程中强制注入、删除或改写用户配置项。
  - 特别是不得强制设置 `loginFailExit = false`。
  - 用户配置应保持原样，项目只负责校验和运行。
- 更新配置必须具备失败保护：
  1. 先写入临时文件并完成校验。
  2. 替换正式配置前保留旧配置备份。
  3. 新配置启动失败时恢复旧配置。
  4. 原实例正在运行时，回滚后要尝试恢复旧实例。
- 配置更新、实例创建、删除、重命名、扫描和启停等互相影响的变更操作必须避免并发交错。
- 实例名称必须继续遵守 `ValidName` 规则：支持中文、字母、数字、空格、`.`、`_`、`-`，禁止路径分隔符、危险保留名和首尾点号。
- 停止状态通过 `data/state.json` 持久化，不能因为普通重启而自动清除。

## 4. 实例与状态

- 实例状态使用 `running`、`stopped`、`exited`。
- 单个实例启动或退出不得影响其他实例。
- 运行中的实例保存有效配置后需要重启应用配置。
- 停止实例时必须更新持久化停止状态；启动实例时清除对应停止标记。
- 修改实例名称时要同时处理：配置文件名、内存注册表、停止状态和页面显示名称。

## 5. API 与安全

- API 路径统一使用 `/api/...`，JSON 响应默认禁止浏览器缓存（`Cache-Control: no-store`）。
- 新增 API 时同步更新 README 的 REST API 表格。
- 管理 API 包含配置读取、配置写入、日志读取、启停和删除能力，除登录、会话、退出和版本接口外都必须登录。
- 默认登录账号为 `admin` / `admin123`；支持通过 `FRP_MORE_USERNAME` 和 `FRP_MORE_PASSWORD` 覆盖，生产环境必须修改默认密码。
- 登录使用 HttpOnly、SameSite=Lax 会话 Cookie；新增管理 API 时必须保持认证中间件保护。
- 不要在页面或公开接口重新暴露已隐藏的服务端地址、认证 token 等敏感信息。
- 修改 API 行为时要考虑请求大小限制、错误状态码和并发请求。

## 6. 前端规则

- 前端保持单文件、无外部运行时依赖的设计。
- 页面必须支持窄屏和移动端，避免横向溢出；操作按钮不能被挤压成竖排文字。
- 实例卡片操作保持紧凑，避免按钮换行破坏布局。
- 日志弹窗需要保持工具栏不重叠，并支持关键字筛选和自动刷新。
- 页面显示服务端地址时要遵循当前的隐藏策略，不要仅为了调试重新展示。
- 修改页面后同步检查 `doc/preview.png` 和 README 描述是否过时。
- 前端改动应避免 XSS；来自 API 的内容必须转义或使用 `textContent`。

## 7. 日志

- FRP 使用全局日志器；日志通过 `internal/logbuf` 保存最近有限行数并展示到页面。
- 不要假设日志天然按实例隔离。
- 日志缓冲区必须保持有界、线程安全。
- 修改日志格式或查看器时，同时验证 stdout、页面日志和过滤功能。

## 8. 部署与发布

### Docker

- 默认使用 host 网络，管理端口默认 `1332`。
- Docker 镜像构建目标为 `linux/amd64` 和 `linux/arm64`。
- 配置和状态必须放在 `/data` 挂载目录。
- `install.sh` 要兼容没有 `hostname -I` 的环境，不能因为获取本机 IP 失败而中断部署。

### fnOS

- fnOS 应用包目标为 x86_64。
- 应用内二进制运行路径是 `${TRIM_APPDEST}/bin/frp-more`。
- 生命周期脚本必须覆盖安装、升级、启动、停止、状态和卸载场景。
- 修改打包路径或启动参数后必须实际检查 fpk 启动流程。

### 版本发布

- 发布标签使用 `v*` 格式，例如 `v0.1.11`。
- `v0.1` 这类短版本标签也必须保持镜像标签兼容。
- 发布前同步检查：应用版本、README、Docker 镜像标签、fnOS manifest 和 Release 附件。
- 版本发布必须创建对应的 Git 标签，例如版本 `0.1.11` 对应标签 `v0.1.11`。
- 发布时必须同时推送代码分支和版本标签：`git push origin main`、`git push origin vX.Y.Z`。
- 推送版本标签后必须监测 GitHub Actions 构建进度，确认 Docker 镜像和 fnOS 应用包构建结果；失败时查看日志并反馈原因，不得只推送不验证。
- GitHub Actions 必须先创建对应 Release，再上传 `.fpk`；不能只上传 Artifact 或等待一个不存在的 Release。
- 发布验收必须同时确认：Actions 所有 job 成功、Release 页面存在、Release 附件包含正确版本的 `.fpk`；Docker 镜像应在 GHCR 中确认，不能误以为它会出现在 Release 附件里。
- `.fpk` 和运行时 `data/` 不作为普通源码提交。

## 9. 标准版本发布流程

1. 更新应用版本号，并同步检查 README、fnOS manifest 和镜像标签规则。
2. 执行 `gofmt`、`go test ./...`、`go vet ./...` 和 `go build .`。
3. 使用中文提交信息提交版本改动。
4. 在该发布提交上创建 `vX.Y.Z` 标签。
5. 同时推送 `main` 分支和版本标签。
6. 监测对应 GitHub Actions，确认 Docker 和 fnOS 构建完成。
7. 检查 GitHub Release 和 `.fpk` 附件；如果没有 Release 或附件，立即检查工作流并修复，不得把“Actions 成功”当作“发布完成”。

## 10. 测试与提交前检查

每次修改 Go 代码至少执行：

```bash
gofmt -w <修改过的 Go 文件>
go test ./...
go vet ./...
go build .
```

涉及 Manager、配置替换、生命周期或 API 的改动必须增加或更新测试。重点覆盖：

- 配置非法时原文件不变。
- 配置更新失败时可以恢复旧配置。
- 实例重命名后旧文件不存在、新文件存在且注册表正确。
- 停止状态重启后仍然有效。
- 并发操作不会覆盖配置。

提交原则：

- 一个提交尽量只解决一个问题。
- Git 提交信息统一使用中文。功能新增使用“新增：……”格式，缺陷修复使用“修复：……”格式，版本发布使用“发布 vX.Y.Z：……”格式。
- 修改页面、API 或部署行为时同步更新文档。

## 11. 已知限制

- FRP 的全局 logger 会让多个实例的日志混在一起。
- `frp/client` 是内部 API，不承诺跨版本稳定。
- 管理面板已有基础登录认证，但默认账号密码仅适合首次进入；生产环境必须修改密码，并限制 1332 端口访问范围。


